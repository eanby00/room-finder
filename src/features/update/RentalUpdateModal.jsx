import Modal from '../../components/Modal';

function RentalUpdateModal({ onClose }) {
  return (
    <Modal title="대관 데이터 갱신" onClose={onClose}>
      <p>대관 데이터를 갱신하는 기능을 여기에 추가할 예정입니다.</p>
    </Modal>
  );
}

export default RentalUpdateModal;
